import { Route } from '@angular/router';
import { NavbarComponent } from './navbar.component';

// Navbar is not a navigable page; export an empty array for consistency
export const navbarRoutes: Route[] = [];
